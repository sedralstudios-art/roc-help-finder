node_modules
dist
.DS_Store
# call-data/ is the dialer pipeline — scripts, phone queue, recordings.
# Everything local except HANDOFF.md, which stays tracked as the operational doc.
call-data/*
!call-data/HANDOFF.md
test_recording.*
geodata/
